@model IEnumerable<GamingZone.Models.Event>

@{
    ViewBag.Title = "Events";
}

<div class="row">
    <div class="col-md-12">
        <!-- Form Elements -->
        <div class="panel panel-default">
            <div class="panel-heading">
                Events
            </div>
            <div style="color:white; background-color:#222323" class="panel-body">
                <div class="row">
                    <div class="col-md-12">
                        <!--Div End-->
                        @*@using (Html.BeginForm())
                        {

                            @Html.AntiForgeryToken()

                            <div class="form-horizontal">
                                <div class="form-group">
                                    @Html.Label("Search:", new { @class = "control-label col-md-2" })

                                    <div class="col-md-8">


                                        @Html.TextBox("SearchCategory", null, new { @class = " form-control ", placeholder = "Search Categories...", style = " max-width:100%;" })

                                    </div>
                                    <div class="col-md-2">
                                        <button type="submit" class="btn btn-primary" style="background-color: purple; color: white; background-image: linear-gradient(purple,black);">
                                            <i class="fa fa-search"></i>
                                            Search

                                        </button>


                                    </div>

                                </div>

                            </div>


                        }*@
                        <p>
                            @Html.ActionLink(" Create New Event", "Create", "", new { @class = "btn btn-success far fa-plus-square", style = "background-color: #0d973e; background-image: linear-gradient(#0d973e,black);" })
                        </p>
                        <br />

                        <table class="table table-bordered " id="dataTables-example">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.Subject)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Description)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Subject)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Description)
                                    </td>
                                    <td>
                                        @Html.ActionLink(" Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-primary fas fa-edit" })
                                        @Html.ActionLink(" Details", "Details", new { id = item.Id }, new { @class = "btn btn-warning far fa-list-alt" })
                                        @Html.ActionLink(" Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger far fa-trash-alt" })
                                    </td>
                                </tr>
                            }

                        </table>
                        <!--Code End-->
                    </div>
                </div>
            </div>
            <!-- End Form Elements -->
        </div>
    </div>

</div>